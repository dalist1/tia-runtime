{
 "name": "tia",
 "version": "0.5.0",
 "description": "tia-runtime — Terminal Interactive Agents runtime: an open, sandboxed fast path for terminal coding agents (in-process zero-spawn read/write/edit tools with byte-verified atomic writes, compiled pi startup, slim JSON streaming)",
 "scripts": {
  "bench:latency": "bun bench/latency.ts",
  "bench:runtime": "bun bench/runtime-boundaries.ts",
  "bench:tools": "bun bench/tool-benchmark.ts",
  "bench:feedback": "bash bench/feedback-loop.sh",
  "bench:writer": "bun bench/slim-stream-writer-burst.ts 1000 10000",
  "install:zig": "bash scripts/install-zig.sh",
  "lint": "oxlint -c .oxlintrc.json scripts/*.ts bench/*.ts --deny-warnings",
  "typecheck": "tsc --noEmit",
  "format": "oxfmt --check scripts/*.ts bench/*.ts bench/history/*.json package.json .oxlintrc.json tsconfig.json",
  "format:write": "oxfmt --write scripts/*.ts bench/*.ts bench/history/*.json package.json .oxlintrc.json tsconfig.json"
 },
 "devDependencies": {
  "@earendil-works/pi-ai": "0.85.0",
  "@earendil-works/pi-coding-agent": "0.85.0",
  "@earendil-works/pi-server": "0.85.0",
  "@earendil-works/pi-tui": "0.85.0",
  "@sinclair/typebox": "^0.34.52",
  "@types/node": "^26.4.1",
  "bun-types": "canary",
  "oxfmt": "latest",
  "oxlint": "latest",
  "typescript": "next"
 },
 "packageManager": "bun@1.4.3-canary.1"
}
